const { FollowModel } = require("../schemas/Follow")

const Follow = {
    create: async ({ newFollow }) => {

    },

    findById: async ({ user_id }) => {

    },

    findAll: () => {

    },

    update: async ({ user_id, fieldToUpdate, newValue }) => {

    }
}

module.exports = { Follow }